import getValue from 'overlay/utils/get-value';
import { module, test } from 'qunit';

module('Unit | Utility | get-value', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getValue();
    assert.ok(result);
  });
});
