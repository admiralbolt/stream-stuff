import commandsListCommand from 'overlay/utils/commands/list-command';
import { module, test } from 'qunit';

module('Unit | Utility | commands/list-command', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = commandsListCommand();
    assert.ok(result);
  });
});
