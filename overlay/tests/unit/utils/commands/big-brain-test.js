import commandsBigBrain from 'overlay/utils/commands/big-brain';
import { module, test } from 'qunit';

module('Unit | Utility | commands/big-brain', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = commandsBigBrain();
    assert.ok(result);
  });
});
