import commandsSmallBrain from 'overlay/utils/commands/small-brain';
import { module, test } from 'qunit';

module('Unit | Utility | commands/small-brain', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = commandsSmallBrain();
    assert.ok(result);
  });
});
